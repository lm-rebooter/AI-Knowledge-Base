/**
 * This seed file documents the kind of starter data the project expects.
 * You can later wire it to Prisma once the database workflow is ready.
 */
async function main() {
  console.log("Seed placeholder: create demo users, knowledge bases, and documents.");
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
